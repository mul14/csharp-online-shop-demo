# Online Shop Demo with ASP.Net Core - C#

```
dotnet new mvc --auth Individual --use-local-db -o OnlineShop -n OnlineShop
cd OnlineShop
dotnet new gitignore
git add .
git commit -m 'Initial commit'
```
